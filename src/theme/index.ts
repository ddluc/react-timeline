import { theme  as DEFAULT_THEME } from './default'; 
import { theme  as CONDENSED_THEME } from './condensed'; 
import { theme  as DARK_THEME } from './dark'; 

export default { 
  default: DEFAULT_THEME, 
  condensed: CONDENSED_THEME, 
  dark: DARK_THEME, 
}; 