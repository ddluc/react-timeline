import styled, { Theme } from 'styled-components';

export interface TimelineContainerProps { }; 

export interface ThemedProps extends TimelineContainerProps {
  theme: Theme;
}

export const TimelineContainer = styled.div`
  
`; 

